# .streamlit/secrets.toml (LOCAL — add to .gitignore!)

[api_keys]
openai_key = 'sk-xxxxxxxxxxxxxxxxxxxxxxxx'
huggingface_token = 'hf_xxxxxxxxxxxxxxxx'
[database]
host = 'localhost'
port = 5432
password = 'mypassword'
# app.py — Access secrets safely
import streamlit as st
# Accessing secrets
openai_key = st.secrets['api_keys']['openai_key']
db_pass = st.secrets['database']['password']
# For Community Cloud:
# Go to app settings -> Secrets -> paste your secrets.toml content
# The file is encrypted and never visible in your repo